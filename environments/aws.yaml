tosca_definitions_version: cloudify_dsl_1_3

imports:
  - https://cloudify.co/spec/cloudify/6.1.0/types.yaml

capabilities:
  cloud_type:
    value: aws
  aws_access_key_id:
    value: { get_secret: aws_access_key_id }
  aws_keypair:
    value: { get_secret: aws_keypair }
  aws_secret_access_key:
    value: { get_secret: aws_secret_access_key }
 
